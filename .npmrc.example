# Use this only if your org packages are hosted in a private npm registry.
# Replace the registry URL and token source with your real values.

@thunderstrikeco:registry=https://registry.example.com
//registry.example.com/:_authToken=${NPM_TOKEN}
