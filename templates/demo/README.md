# Bundled demo template

This folder is copied when someone runs:

```bash
vp create @thunderstrikeco:demo
```

Replace this folder with real template files, or delete the `demo` entry from `createConfig.templates`.
